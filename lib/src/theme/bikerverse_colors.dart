import 'package:flutter/material.dart';

class BikerverseColors {
  // --- Greens (deeper premium emerald) ---
  static const Color accent = Color(0xFF1FB35C); // primary green
  static const Color brightGreen = Color(0xFF39D982); // highlights / focus / check
  static const Color deepGreen = Color(0xFF13854A); // gradient / pressed end
  static const Color onGreen = Color(0xFF06140C); // text on green (near-black)
  static const Color accentSoft = Color(0xFF1B4E32);

  // Green tints derived from the primary green.
  static const Color accentGlow = Color(0x2E1FB35C); // rgba(31,179,92,0.18)
  static const Color accentFill = Color(0x1F1FB35C); // rgba(31,179,92,0.12)

  // --- Charcoal / backgrounds (no pure #000) ---
  static const Color background = Color(0xFF0F1316);
  static const Color glowTop = Color(0xFF16201C); // top radial glow start
  static const Color surface = Color(0xFF141A1D);
  static const Color card = Color(0xFF161B1E);
  static const Color cardElevated = Color(0x07FFFFFF); // rgba(255,255,255,0.025)
  static const Color outline = Color(0x1CFFFFFF); // hairline rgba(255,255,255,0.11)
  static const Color divider = Color(0x12FFFFFF); // rgba(255,255,255,0.07)

  // --- Text on dark ---
  static const Color textPrimary = Color(0xFFEDF1EF);
  static const Color textSecondary = Color(0x99E7EFEB); // rgba(231,239,235,0.60)
  static const Color textMuted = Color(0x61E7EFEB); // rgba(231,239,235,0.38)

  static const Color iconButtonBg = Color(0xFF161B1E);
  static const Color chipBg = Color(0xFF123825);
  static const Color chipBorder = Color(0xFF1F5B3A);
  static const Color priceGreen = Color(0xFF1FB35C);

  // Light Theme Colors
  static const Color lightBackground = Color(0xFFF2F5F8); // Light greyish white
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightCard = Color(0xFFFFFFFF);
  static const Color lightCardElevated = Color(0xFFF5F5F5);
  static const Color lightTextPrimary = Color(0xFF1A1D1E);
  static const Color lightTextSecondary = Color(0xFF6C757D);
  static const Color lightTextMuted = Color(0xFFADB5BD);
  static const Color lightOutline = Color(0xFFE0E0E0);
  static const Color lightIconButtonBg = Color(0xFFEEEEEE);
  static const Color lightChipBg = Color(0xFFE8F5E9); // Light green bg
  static const Color lightChipBorder = Color(0xFFA5D6A7);

}
